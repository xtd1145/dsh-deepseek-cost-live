# dsh-deepseek-cost-live bundle patch: mount the host plugin under its package
# name, resolved through the profile's node_modules.
- insert:
    - id: dsh-deepseek-cost-live
      name: dsh-deepseek-cost-live
